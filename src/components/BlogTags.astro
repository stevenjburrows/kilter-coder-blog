---
const { tags } = Astro.props;
---

<div class="tags">
  {
    tags?.map((tag) => (
      <>
        <a class="tag" href={`/tag/${tag.slug}`}>
          #{tag.name}
        </a>
        {` `}
      </>
    ))
  }
</div>
